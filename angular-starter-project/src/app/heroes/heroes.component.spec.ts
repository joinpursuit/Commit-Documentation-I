
import { HeroesComponent } from './heroes.component';

describe('HeroesComponent logged in as agent', () => {
  let component: HeroesComponent;

  beforeAll(async () => {
    // login
  });

  describe('search by address', () => {
    beforeEach(() => {
      // search by address
    })

    it(' should render all columns');

    it(' should render mobile view')
  });


  it('should create', () => {
    // mount component

    // set up search

    // expect to be rendered correctly
    expect(component).toBeTruthy();
  });

  afterAll(async () => {
    // logout
  })
});

describe('Heroes component logged in as lender', () => {

});
